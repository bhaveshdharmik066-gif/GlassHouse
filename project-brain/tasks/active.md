<!-- # Active Task
Building Phase 4: LoopAgent + CLI

## Objectives
- Build `LoopAgent`: Orchestrates the Attack-Strategy Agent (Phase 2) and Evaluator Agent (Phase 3).
- Implement a loop that takes failures from the Evaluator and feeds them back into the Attack-Strategy Agent for self-correction.
- Build the final `redteam_cli.py` to run the entire pipeline end-to-end. -->