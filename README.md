# GlassHouse
A meta-agent system whose entire job is attacking other agents — given any target agent's tool list and system prompt, it generates and runs adversarial prompt-injection attempts, reporting exactly which tools are exploitable.
